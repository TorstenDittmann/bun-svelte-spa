{
	"name": "svelte-bun-benchmark",
	"version": "0.1.0",
	"private": true,
	"description": "Benchmark: 1000 static routes",
	"type": "module",
	"scripts": {
		"build": "bun run build.ts",
		"generate": "bun run generate.ts"
	},
	"dependencies": {
		"bun-svelte-spa": "workspace:*",
		"svelte": "^5.44.0"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5.9.3"
	}
}
