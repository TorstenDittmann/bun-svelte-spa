{
	"name": "svelte-bun-stress-test",
	"version": "0.1.0",
	"description": "Stress test example with 5000 Svelte components for build speed testing",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bun run build.ts",
		"dev": "bun run dev.ts",
		"generate": "bun run generate-components.ts",
		"clean": "rm -rf src/components/generated && rm -rf dist"
	},
	"dependencies": {
		"launch.css": "^1.1.6",
		"svelte": "^5.35.6",
		"bun-svelte-spa": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5"
	}
}
